name: Android Server Build Workflow
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4
    - name: Setup .NET Environment
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
    - name: Recreate Clean Solution and Restore
      run: |
        dotnet new sln
        find . -name "*.csproj" -exec dotnet sln *.sln add {} +
        dotnet restore *.sln
    - name: Compile and Publish Server
      run: |
        dotnet publish *.sln -c Release -o ./ServerOut/ --no-restore -p:GenerateAssemblyInfo=false -p:Deterministic=false
