name: build-server-and-ape

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Kiểm tra và lấy mã nguồn sạch lỗi
        uses: actions/checkout@v4

      - name: Cấu hình môi trường .NET 10.0 si�~u mượt
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Khôi phục NuGet thẳng vào dự án Startup
        run: dotnet/setup-dotnet@v4

      - name: Bi&#E7;n dịch và đống gói Server từ d�k án Startup
        run: |
          dotnet restore
          dotnet publish Startup/MUnique.OpenMU.Startup.csproj -c Release -o ./ServerOut/ -p:BuildProjectReferences=true

      - name: Đóng go#i sản phẩm Server sảch lỗi gừi đám mây
        uses: actions/upload-artifact@v4
        with:
          name: OpenMU-Server-Build
          path: ./ServerOut/